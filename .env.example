# LingoQuest - Local Development Environment Variables

# PostgreSQL Database Configuration
# You'll need a local PostgreSQL instance running
DATABASE_URL=postgresql://username:password@localhost:5432/lingoquest
PGHOST=localhost
PGPORT=5432
PGUSER=your_postgres_username
PGPASSWORD=your_postgres_password
PGDATABASE=lingoquest

# Session Secret
# Generate a secure random string for session encryption
# You can generate one with: openssl rand -base64 32
SESSION_SECRET=your-secure-random-session-secret-here-change-this

# Optional: Node Environment
NODE_ENV=development
